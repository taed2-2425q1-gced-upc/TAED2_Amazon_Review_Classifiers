"""
Initialization file for the src package.

This file allows the src package to import necessary configurations and
make them accessible to other modules. It serves as an entry point to
the package, ensuring proper configuration management.

Imports:
    config: Module containing configurations for file paths, directories,
    and other settings required for the project.
"""
from src import config
